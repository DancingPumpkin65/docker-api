<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Items</title>
</head>
<body>
    <h1>Item List</h1>
    <ul>
        <% items.forEach(item => { %>
            <li><%= item.name %> (ID: <%= item.id %>, Status Code: <%= item.statusCode %>)</li>
        <% }); %>
    </ul>
</body>
</html>
